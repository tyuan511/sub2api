# Implementation plan

## 1. 冻结契约和基线

> 2026-09-05 用户调度滑块扩展见第 14 节，旧的三预设/固定 50% Key 门槛以补充契约为准。

- [x] 1.1 完成多分组路由完整行为规范，覆盖顺序/智能、成功率硬门槛、价格归一化、粘性、熔断、恢复、缓存补偿和实际归属
- [x] 1.2 盘点现有 API Key、鉴权缓存、组内账号调度、usage/计费和 Channel Monitor V2 数据流
- [x] 1.3 明确 PostgreSQL、Redis、进程内存的数据职责和热路径性能预算
- [x] 1.4 确认历史 `group_id = NULL` 当前表示未限定账号池，并写入兼容设计
- [x] 1.5 保存变更前后端定向测试、全量构建和单分组请求延迟基线
- [x] 1.6 将实现拆为可独立评审和回滚的阶段

## 2. 数据模型与控制面兼容层

- [x] 2.1 新增 `api_keys.schedule_mode`、`smart_preference`、`route_version`，带默认值和检查约束
- [x] 2.2 新增 `api_key_group_routes`、唯一约束、反向索引、时间字段和外键行为
- [x] 2.3 幂等回填非空旧 `group_id` 为 priority=0 路由，不改变历史 null Key
- [x] 2.4 新增 API Key 路由配置失效 outbox 表、唯一事件键、领取索引和保留字段
- [x] 2.5 更新 Ent schema 并生成代码，增加关系加载和 service domain 模型
- [x] 2.6 在 Repository 中事务创建/替换路由，同步旧 `group_id`、递增 route_version、执行 expected_route_version CAS 并写 outbox
- [x] 2.7 扩展创建/更新/读取 DTO；兼容只传 `group_id`，拒绝新旧首项冲突和非法枚举/顺序/数量
- [x] 2.8 校验所有候选组同平台、同 subscription_type 且用户有权绑定，错误码稳定
- [x] 2.9 扩展鉴权 L2/L1 快照并升级 schema version，一次加载最多 8 个候选组所需投影
- [x] 2.10 增加 migration、Repository、service、handler、快照往返和旧客户端兼容测试

## 3. 用户端多分组配置体验

- [x] 3.1 扩展前端 ApiKey 类型和 API 请求，保留旧 `group_id`
- [x] 3.2 创建 Key 时支持同平台候选组多选、拖拽/按钮排序和最多 8 项限制
- [x] 3.3 编辑 Key 时原子替换候选集合，并携带 `expected_route_version` 处理 409 冲突
- [x] 3.4 支持 sequential/smart 和 price/speed/balanced 联动，解释成功率优先规则
- [x] 3.5 显示候选组状态、倍率和不可选原因；已停用但历史绑定的组保持可识别
- [x] 3.6 添加中英文 i18n、键盘操作、窄屏和并发编辑 Vitest
- [x] 3.7 在智能调度界面披露持续优化范围：只优化用户已选候选、硬约束不变、健康粘性不受实验影响，并展示当前策略版本与更新时间

## 4. 顺序分组调度与实际归属

- [x] 4.1 新增功能开关和 `RouteCoordinator`，单分组/功能关闭继续走原路径
- [x] 4.2 从鉴权快照构造候选，过滤权限、状态、平台、计费类型、模型和端点能力
- [x] 4.3 sequential 按 priority 稳定排序，并把选定组写入请求级路由上下文
- [x] 4.4 每组先复用现有组内账号选择和重试，组内穷尽后才允许进入下一组
- [x] 4.5 建立统一失败分类，客户端错误、余额/权限、内容拒绝和不可安全重放错误不得换组
- [x] 4.6 建立统一语义输出门闩；HTTP/SSE/WS/媒体一旦有语义输出禁止跨组重放
- [x] 4.7 计费资格、订阅、利润控制、模型映射和账号并发全部基于当前实际组执行
- [x] 4.8 usage、ops log 和账单记录最终实际组，并记录 initial_group_id/route_version/switch_count
- [x] 4.9 添加单组等价、两组切换、全部失败、失败分类和实际归属测试

## 5. Redis 路由粘性、熔断和恢复

- [x] 5.1 定义 Cluster-safe hash tag key，所有状态包含 api_key_id 和 route_version
- [x] 5.2 实现组级会话粘性，与现有组内账号粘性形成两级粘性
- [x] 5.3 使用 Lua 原子维护滚动成功/失败计数、CLOSED/OPEN/HALF_OPEN/RECOVERING 和 TTL
- [x] 5.4 任一有效窗口成功率 `<50%` 立即 OPEN，`=50%` 不因该规则熔断
- [x] 5.5 实现冷却、单租约半开探测、防探测风暴和恢复失败回到 OPEN
- [x] 5.6 实现排空式恢复：新会话回主组，活跃备用会话等待 TTL，自身失败才提前迁移
- [x] 5.7 route_version 和依赖版本变化后旧状态不得影响新配置
- [x] 5.8 Redis 不可用时采用明确、可观测的保守降级，不把新路由空集传入全局账号池
- [x] 5.9 添加多实例并发、Lua 原子性、TTL、版本隔离、恢复排空和 Redis 故障测试

## 6. 监控聚合、价格归一化与共享评分

- [x] 6.1 复用分钟桶口径并独立维护路由聚合，保存 group/model_family/endpoint 的成功、失败类别、TTFT、总耗时、容量和 actual token 结构，不依赖监控 V1/V2/off
- [x] 6.2 明确分组访问分母和有效窗口，低流量使用先验/最小样本并标记置信度
- [x] 6.3 实现逻辑输入、窗口整体 cache hit rate、actual usage 重价和 full-cache-1x 归一化倍率
- [x] 6.4 验证 ForceCacheBilling、billable_usage 和缓存补偿不会污染价格评分输入
- [x] 6.5 实现 1h → 24h → 平台基线 → 名义价格回退和置信区间
- [x] 6.6 实现单例 `RoutingScoreBuilder`、有界窗口/游标增量聚合、版本化 Redis 快照和 current 指针；最近 10 分钟修复、30 分钟历史块、24h 上限，重启幂等重算
- [x] 6.7 实例原子加载评分快照；TTL 覆盖至少三个构建周期并支持旧快照安全降级
- [x] 6.8 增加公式单测、真实 token 结构样例、窗口回退、builder 接管和快照一致性测试

## 7. 智能动态排序与预估倍率

- [x] 7.1 实现 success/price/speed/capacity 统一维度和版本化权重配置
- [x] 7.2 三种倾向只改变权重，全部先执行健康/成功率硬门槛
- [x] 7.3 对最多 8 个候选应用用户专属价格投影、归一化和确定性 tie-break
- [x] 7.4 单请求固定评分版本，重试过程不得因后台刷新改变顺序
- [x] 7.5 计算 expected/low/high/reference/window/confidence/updated_at 预估倍率并明确非报价承诺
- [x] 7.6 返回 current_rank、score、score_breakdown、健康、成功率、延迟、缓存和价格解释
- [x] 7.7 添加不同权重、低成功率廉价组、评分并列、用户倍率和快照刷新测试

## 8. 缓存冷启动补偿与异常路由事实

- [x] 8.1 将 usage 的供应商 `actual_usage` 与用户 `billable_usage` 明确分列
- [x] 8.2 仅对系统触发且可归因的组间切换标记 `cache_cold_due_to_failover`
- [x] 8.3 实现有界补偿 token/金额、原因和审计字段，不改实际组和真实成本
- [x] 8.4 新增 `routing_attempts`/决策事实表及必要索引、分级保留期和脱敏约束
- [x] 8.5 异常/换组/探测事件写有界 Redis Stream，消费者批量幂等落 PostgreSQL
- [x] 8.6 Redis 不可用时使用有界内存降级队列；拥塞优先保留失败和部分计费事件
- [x] 8.7 正常首选成功只聚合/采样，禁止同步逐请求写完整尝试链
- [x] 8.8 添加补偿上限、重复事件、队列满、消费者重启和财务事实不丢失测试

## 9. Outbox、失效、观测和运维控制

- [x] 9.1 实现 outbox worker 的 claim/lease/retry/idempotent delivery 和保留清理
- [x] 9.2 删除 Redis 鉴权/路由快照并发布 L1 失效；遗漏通知由版本校验兜底
- [x] 9.3 分组停用、删除、权限、订阅、价格和能力变化触发反向依赖失效
- [x] 9.4 增加候选分布、换组率、breaker 状态、探测、评分年龄、缓存命中和 dropped event 指标
- [x] 9.5 增加按请求可关联且不含 Key 明文/凭据/正文的结构化日志
- [x] 9.6 提供管理员只读路由解释和受审计的 breaker/粘性清理操作
- [x] 9.7 添加 outbox 崩溃恢复、重复投递、Pub/Sub 丢失和依赖失效集成测试

## 10. 持续优化事实与版本基础（首发必备）

- [x] 10.1 定义可按时间点重放的采样决策事实：记录有界候选集、准入结果、特征值、置信度、分项分数、排序、选择与结果可见性，未选择候选明确标记为 unobserved
- [x] 10.2 仅允许数值、枚举和分桶特征进入事实流；禁止写入 prompt、response、API Key 明文、上游凭据和可反推出敏感正文的字段
- [x] 10.3 分离并记录 `route_version`、strategy/score/feature/model/experiment version；策略或模型切换不得递增 route_version、清空健康粘性或改变进行中请求
- [x] 10.4 对观察采样精确记录 `sample_probability`；仅在真实受控探索时记录非平凡 `action_propensity`，确定性日志不得伪装成随机对照数据
- [x] 10.5 建立决策、结果、错误、计费和延迟事件的关联键、优先级、幂等语义、保留期与数据完整性指标；事件链路不健康时禁止依赖它做晋级
- [x] 10.6 在 PostgreSQL 建立不可变策略、评分、特征、模型和实验版本注册表，支持 draft/shadow/canary/active/paused/retired 生命周期及 lineage/schema/checksum/dependency 元数据
- [x] 10.7 在 Redis 原子维护各平台/模型/端点的 active/canary/baseline 指针，实例只加载校验通过的完整不可变对象到本地内存，加载失败确定性回退 baseline
- [x] 10.8 固化规则评分 baseline 与全局 kill switch；持续优化组件不可绕过候选白名单、`<50%` 熔断、重试安全、健康粘性和实际组计费硬约束
- [x] 10.9 添加时间点回放、未来信息泄漏、采样概率、版本独立、敏感字段、对象校验、指针原子切换和 baseline 回退测试
- [x] 10.10 隔离请求级安全控制、分钟级动态评分、离线策略校准和按需产品治理控制环；慢控制环只能生成不可变 draft，不得直接写熔断、粘性或单请求状态
- [x] 10.11 为训练/回放数据集保存查询版本、时间边界、特征 schema、采样与排除规则、checksum，并用决策时点快照或 point-in-time join 防止未来信息泄漏
- [x] 10.12 固化 Builder、网关和事件消费者的向后兼容特征契约；未知字段可忽略，关键字段缺失、NaN、越界或 checksum/schema 不兼容时拒绝加载并回退 baseline

## 11. Shadow、灰度晋级与自动回滚（首发必备）

- [x] 11.1 实现 side-effect-free shadow：复用同一 `routing_decision_id` 计算候选排序差异，但不得申请容量、写粘性、改变实际组、计费或请求结果
- [x] 11.2 canary 使用稳定的用户/Key 分桶且仅影响新会话；已有健康粘性会话继续原选择，实验分桶和策略版本可审计
- [x] 11.3 为 price/speed/balanced 定义版本化偏好包络与共同成功率护栏；任何倾向都不得用成功率换价格或速度
- [x] 11.4 定义偏好专属晋级指标：price 看每次成功成本，speed 看成功完成耗时/TTFT 且受价格上限约束，balanced 使用版本化综合体验损失
- [x] 11.5 晋级必须同时满足最小样本、观察期、置信区间和平台/模型/端点/关键 Key 切片门槛；关键切片退化阻止晋级
- [x] 11.6 实现候选版本数据校验 → 离线回放 → shadow → canary → active 流程和人工审批/审计记录
- [x] 11.7 硬护栏触发时自动停止 canary、原子回退 baseline 并记录结构化原因；决策事件链路不完整时自动暂停 canary 与探索
- [x] 11.8 控制 Prometheus 版本标签基数，只暴露 baseline/active/canary/近期版本；完整历史版本保留在诊断事实与 PostgreSQL
- [x] 11.9 管理端展示生效版本、实验桶、shadow/canary 差异、关键切片、晋级证据和自动回滚原因，并提供审计化暂停/恢复/回退操作
- [x] 11.10 添加 shadow 无副作用、稳定分桶、新旧会话隔离、偏好护栏、关键切片阻断、自动回滚和指标基数测试
- [x] 11.11 基于完整实际尝试链计算 `FailureRisk`、每次最终成功成本、到最终成功 TTFT/耗时和 `StabilityLoss`；同一 strategy_version 固定线上正向评分与离线体验损失的可审计映射
- [x] 11.12 建立按偏好的持续优化指标与数据质量面板，报告样本量、置信区间、观察周期、实验覆盖、P95/P99、校准误差、特征缺失和 drift，禁止只凭总平均值自动晋级

## 12. 稳定性控制（首发必备）与学习增强（满足前提后开放）

- [x] 12.1 对评分应用置信度收缩、平滑、最小分差、滞回、最短驻留时间和单位时间流量变化上限；`RECOVERING` 必须渐进放量，防止振荡与赢家锁定
- [x] 12.2 将容量溢出与健康失败分开建模；按共享供应商/账号池/网络等 dependency domain 去重相关失败，避免重复惩罚同一根因
- [x] 12.3 为低流量候选保留被动证据、受控探测与陈旧降权路径，防止长期无流量导致永不恢复
- [x] 12.4 在数据和运行经验满足门槛后，实现平台/模型/端点 baseline → 物理分组 → cohort/Key 残差的分层收缩；新 Key 使用共享基线，低样本禁用或强收缩个性化
- [x] 12.5 限制 Key/cohort 修正幅度、有效期和影响范围；异常 Key 不得污染全局，漂移或质量退化时自动停用残差
- [x] 12.6 在规则 baseline 稳定且数据质量达标后，模型仅增强成功概率、延迟分位数、容量溢出、缓存行为和归一化成本等组件预测；硬过滤、偏好约束和最终排序继续由可解释规则控制
- [x] 12.7 模型在本地以有界延迟和内存运行，校验 lineage/schema/checksum；超时、缺失、陈旧或异常时回退规则组件，训练任务只产出 draft 不得自动上线
- [x] 12.8 受控探索默认关闭；只有准确 propensity、稳定分桶、预算上限、自动停止、baseline 回退和事件完整性全部就绪后，才可在满足硬约束的新会话上独立灰度
- [x] 12.9 禁止把确定性历史数据表述为因果收益；离线评估明确选择偏差，仅在合格探索数据上使用 IPS/DR 等校正
- [x] 12.10 将 Key 级残差、自动生成并晋级权重、bandit/在线探索列为后续能力，不作为首版多分组、规则评分、shadow/canary 和 kill switch 的发布前置条件
- [x] 12.11a 添加振荡、流量突变、相关失败、被动冷启动证据、探索禁用门禁和 canary 自动停止测试
- [x] 12.11b 在 12.4–12.7 学习增强开放时，补充 Key 残差隔离、模型超时/损坏回退和分层冷启动收缩测试

## 13. 全协议覆盖与发布验证

- [x] 13.1 覆盖 Claude Messages、OpenAI Responses/Chat/Embeddings/Images/Live/WS、Gemini、Grok 媒体/音频和批量图片入口
- [x] 13.2 对每个入口证明选择实际组发生在能力、计费和账号调度之前
- [x] 13.3 对流式与 WS 证明语义输出后不会跨组重放，对幂等媒体任务证明不会重复提交
- [x] 13.4 运行后端定向/全量/race 测试、前端 lint/typecheck/Vitest 和生产构建
- [x] 13.5a 提供不泄露 API Key/正文的可重复压测工具，输出成功率、RPS、延迟/TTFT P50/P95/P99 和输入/输出/总 token 吞吐
- [ ] 13.5b 在 hk1 执行单组与 8 组并发压测，验证新增路由开销 P95 不超过 5ms 或批准后的基线预算
- [x] 13.6a 以自动化测试验证 L1 命中零 PostgreSQL、一次 Redis 状态 Pipeline、8 组快照大小、迁移索引契约、固定维度分阶段延迟指标，以及 Builder/事实消费者使用独立有界数据库连接池和可取消查询
- [ ] 13.6b 在 hk1 用 Redis 指标、PostgreSQL EXPLAIN/连接池指标验证真实往返、索引命中和后台任务资源隔离
- [ ] 13.7 灰度顺序：决策事实/数据影子校验 → 单组兼容 → 指定测试 Key sequential → sticky/breaker → smart baseline → shadow → canary → 扩面
- [x] 13.7a hk1 已完成迁移/单组/指定 Key sequential、故障转移粘性、50% 边界、RECOVERING 渐进恢复和 smart baseline；原始证据见 `evidence/hk1-20260905/README.md`
- [ ] 13.7b hk1 补齐 shadow/canary 与分阶段观察窗口；真实上游 502 在旧镜像也复现，需另行恢复并验证成功后才可扩面
- [ ] 13.8 演练关闭功能、停止 builder/outbox、决策事件链路异常、Redis 丢失、模型对象损坏和旧版本回滚，确认规则 baseline 与 `group_id` 镜像仍可服务
- [x] 13.8a hk1 回滚到原镜像 `v20260903.1435`、保留 240–247 迁移，健康与 `group_id` 单组 SSE 冒烟通过，再恢复 `v20260905.0811` 并验证健康
- [ ] 13.9 验证离线时间点重放与线上版本一致，并核对 canary 自动回滚、kill switch、健康粘性保留及实际组计费不变量
- [x] 13.10a 更新本地 OpenSpec 验证证据、运行手册、告警阈值、持续优化数据治理和用户说明，并补齐 Compose/`.env` 的 fail-closed 路由开关与环境变量可达性测试
- [ ] 13.10b 附加 hk1 灰度/演练证据并通过发布门禁后，才可宣布完整上线
- [x] 13.10c 已附 hk1 脱敏证据、无效夹具试验说明、真实上游旧/新镜像 502 对照与测试资源停用记录；无生产部署、无删除业务数据
- [x] 13.11a 修复灰度发现的监控 V1 导致路由桶缺失、优化关闭导致基础失败漏采、普通决策绕过采样；增加窗口/回滚/采样契约和 race 回归
- [ ] 13.11b 补齐语义输出后流式失败的组级健康分母与诊断事实回归：禁止重放已验证，但零费用失败 usage 被排除后仍需明确计入失败，不能只依赖 `actual_cost > 0` 判断成功

## 14. 连续偏好与用户成功率门槛（2026-09-05）

- [x] 14.1 Codex 风格双滑块：胶囊轨道、刻度、圆形滑块、数值、重置、键盘与深色模式
- [x] 14.2 连续比例落地为 50% 成功率 + 10% 容量 + 40% 价格/速度预算，禁止优化制品覆盖用户精确比例
- [x] 14.3 门槛范围 50..95、步长 5、新建默认 80（旧 Key 保持原值）；后端校验、顺序/智能共享健康准入与独立 Key 熔断
- [x] 14.4 迁移 248、Ent、API、鉴权 v26、配置 CAS/outbox 和请求计划接通；旧 Key 与旧客户端兼容
- [x] 14.5 runtime 状态版本独立，比例修改不清空健康粘性，门槛升高复用样本重判；冷却、探测、渐进恢复与迟到版本保护
- [x] 14.6 决策时比例和门槛异步持久化并进入点时重放，不新增按滑块值拆分的共享缓存
- [x] 14.7 本地前后端回归与路由 race、PostgreSQL 隔离事务迁移校验、桌面/390px 深色布局、键盘端点/步进及重置检查
- [x] 14.8a 授权后本地构建并部署 hk1 `v20260905.1016`；迁移 248、前端双滑块资源、配置读取/路由解释、服务健康和旧 Key 默认值/状态版本检查通过，数据库与旧镜像已保留
- [ ] 14.8b 在 hk1 补齐自定义比例/门槛的真实保存回显，以及新门槛下故障转移和恢复的端到端演练；本次发布只做只读冒烟，不修改用户测试 Key 或调用付费上游
- [x] 14.9 按用户反馈将双滑条轨道/圆形滑块由 40px 减为 20px，保留 40px 点击区域并修正端点位置；27 项测试、浏览器拖动/键盘/深色检查通过，部署 hk1 `v20260905.1028` 并核验在线资源和健康
- [x] 14.10 新建/重置门槛改为 80%，创建服务、Repository、Ent 和迁移 249 的默认值保持一致；旧 Key/历史事实/状态版本不回填。相关前后端测试与 PostgreSQL 临时表事务检查通过；已授权部署 hk1 `v20260905.1054`，线上迁移、默认值、25 条既有 Key（含软删除）配置/状态版本逐条比对及健康检查通过
- [x] 14.11 仅选择两个及以上分组时显示调度方式、成功率门槛与智能偏好；单分组固定路由，跳过 Key 级组间熔断/粘性/排序及其 Redis 读写，保留组内账号调度、请求硬检查和失败观测。按配置的启用分组数判定，不能因多分组过滤后剩一个候选而绕过门槛；管理端解释与列表标记同步。后端全量/定向 race、前端 39 项测试、类型/lint/生产构建及 OpenSpec 校验通过；无需迁移。用户授权后本地构建并部署 hk1 `v20260905.1153`，在线资源、版本、服务健康与旧 Key 配置保持检查通过；无可用测试 Key，未做真实路由端到端演练，见 `evidence/hk1-20260905/controls-release.md`。

- [x] 14.12 智能倾向滑块步长从 50 bps（0.5%）改为 500 bps（5%），0..100% 共 21 档；评分公式、默认均衡、成功率门槛及后端兼容精度不变。旧比例在未调整时保留；54 项定向测试、类型/lint、浏览器键盘/拖动/端点/重置验证通过。用户授权后本地构建并部署 hk1 `v20260905.1447`；60 项定向回归、在线步长资源、版本/健康、原名单与业务配置保持核验通过，详见 `evidence/hk1-20260905/step5-release.md`。

## 15. 按用户 ID 开放新功能（2026-09-05）

- [x] 15.1 系统设置「功能开关」增加多分组与智能调度灰度名单，支持用户 ID 精确查询、用户名/邮箱搜索、选择/移除/清空和独立保存；默认空名单，最多 1000 个既有用户，管理员不豁免。
- [x] 15.2 名单使用现有 PostgreSQL settings 保存，提供管理端专用 GET/PUT；认证用户只获取自己的能力布尔值，公共设置不暴露名单；校验 ID、用户存在性与管理员权限并审计保存。
- [x] 15.3 服务端要求环境总开关与名单同时满足，拒绝名单外高级配置；网关和路由解释投影到原 group_id，不改共享鉴权快照、不越过主组硬检查、不启用跨组控制。
- [x] 15.4 名单外恢复旧单分组表单与列表，普通编辑省略隐藏路由字段；移出不删除多组配置，重新加入可恢复，主动改组遵循旧单组替换语义。
- [x] 15.5 实例内不可变 map 原子缓存、5 秒 TTL、合并刷新与 1 秒数据库超时；保存即刷新本实例，读取异常关闭资格，单组请求跳过名单查询，不新增 Redis 或迁移。
- [x] 15.6 后端全量测试、相关 unit/race 与编译通过；前端全量 260 文件/1894 项测试、类型、定向 lint 和生产构建通过；本地卡片亮/深色与增删交互检查通过，补齐规范与发布手册。
- [x] 15.7a 用户授权后本地构建并部署 hk1 `v20260905.1431`；单实例已升级，空名单、接口认证、8 条活动 Key 的单组路由解释、新资源、旧配置/绑定/迁移保持及服务健康只读冒烟通过。保留数据库与旧镜像，不添加灰度用户，us02 未操作；见 `evidence/hk1-20260905/rollout-release.md`。
- [ ] 15.7b 用指定测试用户在 hk1 验证名单加入/移出真实保存、请求端到端与原配置恢复；若扩展到多实例，验证全部升级及 TTL 收敛，不能以本次空名单部署冒烟替代。
- [x] 15.8 交付 `test-plan-hk1.md`：31 项核心用例、30–45 分钟快速验收、灰度/旧单组/5% 滑块/评分/熔断/粘性/恢复/缓存计费与性能门禁，明确测试资源、预算、证据及回退；方案交付不代表环境用例已执行。
